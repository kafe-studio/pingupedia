---
export const prerender = true;
import { getCollection, render } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

export async function getStaticPaths() {
  const posts = await getCollection("blog");
  return posts.map((post) => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await render(post);
const { title, description, pubDate, author } = post.data;
const metadata = { title, description };
---

<BaseLayout {metadata}>
  <article class="pt-32 pb-16 px-6">
    <div class="mx-auto max-w-3xl">
      <header class="mb-10 reveal">
        <h1 class="text-4xl font-extrabold mb-4">{title}</h1>
        <p class="text-sm text-muted-foreground">
          <time datetime={pubDate.toISOString()}>{pubDate.toLocaleDateString("cs")}</time>
          {author && <span> &middot; {author}</span>}
        </p>
      </header>
      <div class="prose prose-invert max-w-none reveal">
        <Content />
      </div>
      <div class="mt-12 pt-8 border-t border-border">
        <a href="/blog/" class="btn-outline py-2 px-6 text-sm">Zpet na blog</a>
      </div>
    </div>
  </article>
</BaseLayout>
